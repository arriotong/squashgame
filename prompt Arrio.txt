I'm breaking my game (a single HTML file) into modular "blocks" that will 
later be merged back together. For this pass, treat the file as containing 
these blocks:

1. [BLOCK NAME(S) TO KEEP FULLY FUNCTIONAL] — e.g. "Enter Court": character 
   select → racquet select → match start → the full gameplay engine (physics, 
   AI, camera, scoring, pause menu). Do not remove or alter any logic here.

2. [BLOCK NAME(S) TO STRIP TO EMPTY PLACEHOLDERS] — e.g. "Settings" and 
   "Controls" screens. Gut their inner content down to just a title and their 
   existing open/close/back buttons (so navigation between screens still 
   works), but remove all actual controls/content.

Rules:
- Don't touch anything outside the named placeholder blocks.
- If removing a block's content orphans any JS that referenced it (event 
  listeners, variable assignments), don't delete that JS — comment it out and 
  leave a clear note listing exactly which gameplay variables/IDs it used to 
  set, so it can be reconnected later.
- Confirm the gameplay still runs standalone with sane hardcoded defaults for 
  anything the stripped block used to control.
- After editing, syntax-check the JS and confirm no broken element references 
  (getElementById/querySelectorAll calls that now return null/empty should be 
  harmless, not throw).
- Give me back the full single HTML file, not a diff.

Here's the file: [attach/paste file]
